'use client';import Link from 'next/link';import {useRouter} from 'next/navigation';import {Flower2,LockKeyhole} from 'lucide-react';import {FormEvent,useState} from 'react';export default function Login(){const router=useRouter();const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError('');const form=new FormData(event.currentTarget);const response=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});if(response.ok)router.push('/admin');else setError('That email or password was not recognised.');setLoading(false)}return <main className="flex min-h-[75vh] items-center justify-center px-5 py-16"><div className="w-full max-w-md rounded-[2rem] bg-white p-8 shadow-sm md:p-10"><Link href="/" className="flex items-center gap-2"><Flower2 className="text-[#c77b70]" size={22}/><span className="serif text-2xl">Petal Paradie</span></Link><p className="label mt-12">Private workspace</p><h1 className="serif mt-3 text-4xl">Welcome back.</h1><p className="mt-3 text-sm text-[#718776]">Sign in to tend your shop.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm">Email<input name="email" type="email" required className="mt-2 w-full rounded-xl border border-[#dedbd1] p-3" placeholder="you@meadowandstem.com"/></label><label className="block text-sm">Password<input name="password" type="password" required className="mt-2 w-full rounded-xl border border-[#dedbd1] p-3" placeholder="Your password"/></label>{error&&<p className="text-sm text-[#b65f56]">{error}</p>}<button disabled={loading} className="btn btn-dark mt-3 w-full disabled:opacity-60"><LockKeyhole size={15}/>{loading?'Signing in...':'Sign in'}</button></form><p className="mt-5 text-center text-xs text-[#718776]">Admin access only. Contact the store owner for help.</p></div></main>}

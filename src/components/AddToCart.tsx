'use client';import {useState} from 'react';import {ShoppingBag,Check} from 'lucide-react';export function AddToCart({disabled=false}:{disabled?:boolean}){const[added,setAdded]=useState(false);return <button disabled={disabled} onClick={()=>{setAdded(true);setTimeout(()=>setAdded(false),1600)}} className="btn btn-dark w-full disabled:bg-[#c6c8c0]">{added?<><Check size={15}/>Added to bag</>:<><ShoppingBag size={15}/>Add to bag</>}</button>}
